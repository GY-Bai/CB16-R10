# 已决定事项与真正未决问题

2026-09-14 精炼。本页只区分用户取舍与执行缺口；实际断点见 [CURRENT_STATE](CURRENT_STATE.md)。

## 不再重复询问的决定

| 事项 | 已确认含义 |
|---|---|
| 失败能否学习 | 允许；成功失败完整事实保留，优胜示范单独筛选 |
| 收益与风险 | 完整计入失败的算术期望取向；不改成生存率/Sharpe/log 主目标 |
| B&H / FLAT 谁作 master | 都不作 master；并列诊断，同合同模型排序与沙盒晋升分开 |
| 账户与换代 | chunk/pause/checkpoint 不免费重置账户或资本；必要状态接续 |
| 人为交易干预 | 不默认加入固定 SL/TP/max-hold/cooldown；环境机械约束仍存在 |
| 起步设备 | CPU-only；GPU 留给后续有依据的扩展，不成为当前运行前置条件 |
| Infra 范围 | Recovery 优先；数据库挑战者按证据触发，Evolution 不阻塞 S1 |

O-01 的经济语义迁移已由 [S0 receipt](../authority/rearchitecture_r11/CB16_R11_POST_CC_S0_RECEIPT_V1.json) 记录 PASS。旧 CC `UNRESOLVED_OWNER_DECISION` 保留历史身份，不是新的 owner 阻塞。

## 参数与研究问题由 Sol 在授权内处理

batch、worker/thread 数、采样比例、网络宽度、观察充分性、有限 horizon 和估计方法，是当前合同约束下的工程参数或后续研究变量。已冻结 S1 常量不再调参；后继研究预注册后验证。不能让用户靠猜数字解决实现缺口，也不能把尚未证明可学习误称为待用户选择。

## 仍需届时决定的边界

改变最高收益/风险目标、追加设备或付费服务、具体破坏性存储操作，以及真实模拟账户/实盘部署和新数据权限，需要相应明确授权。CPU-only 不自动授权清盘、重装或修改全部历史实验。

当前未发现必须由用户再次选择才能进行文档精炼或已有任务准备的新理念问题。已有宿主批准按其精确范围继承，不因本页重新索取；缺失的执行证据由 Sol/DS 完成。
