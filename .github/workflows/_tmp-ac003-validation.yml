name: AC-003 Temporary Validation

on:
  push:
    branches: [ai/r11-ac-003-legacy-freeze-r0]

permissions:
  contents: read

env:
  CB16_RAW_ROOT: /cb16/raw
  CB16_G0_ROOT: /cb16/g0
  CB16_R11_G0_ROOT: /cb16/g0
  CB16_R104_ROOT: /cb16/runtime/r104
  CB16_VERIFIED_VENV: /cb16/venv
  CB16_UV_CACHE_DIR: /cb16/uv-cache
  CB16_CI_WORKER_ROOT: /cb16/worker
  CB16_PROVISION_ENV: /cb16/worker/provision.env

jobs:
  shanxi-preflight:
    uses: ./.github/workflows/_cb16-shanxi-preflight.yml

  ac003-validation:
    name: AC-003 Legacy Freeze Sentinel
    needs: shanxi-preflight
    runs-on: [self-hosted, shanxi-docker-r11]
    timeout-minutes: 15
    steps:
      - name: Checkout source
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Resolve verified canonical Python
        shell: bash
        run: |
          set -euo pipefail
          OUT="$RUNNER_TEMP/cb16_ac003_python.json"
          python3 ci/resolve_verified_r104_python.py --out "$OUT"
          python3 - "$OUT" <<'PY'
          import json, sys
          p=json.load(open(sys.argv[1]))
          assert p['status'] == 'READY', p
          assert p['mode'] == 'VERIFIED_CANONICAL_R104_VENV_REUSE', p
          assert p['python']['network_install_attempted'] is False, p
          print('PYTHON_ENV_READY')
          PY

      - name: Run AC-003 sentinel
        shell: bash
        run: |
          set -euo pipefail
          OUT="$RUNNER_TEMP/cb16_ac003_python.json"
          PYTHON=$(python3 - "$OUT" <<'PY'
          import json, sys
          p=json.load(open(sys.argv[1]))
          print(p['python']['venv'] + '/bin/python')
          PY
          )
          "$PYTHON" -m pytest -q tests/test_actor_critic_legacy_freeze_r0.py

      - name: Prove forbidden holdout absent
        shell: bash
        run: |
          set -euo pipefail
          test ! -e data/2025-09
          test ! -e final_holdout
          echo AC003_VALIDATION_SCOPE_PASS
