# R2 storage qualification / optional accelerators.
# Core R2 pack+manifest path can run on Python stdlib only.

# Faster immutable payload compression; core falls back to zlib if absent.
zstandard>=0.23,<1

# Maintained RocksDB Python binding used for the open-source tiered-backend comparison.
rocksdict>=0.3.29,<0.4

# Optional developer convenience only. Core R2 correctness tests use unittest.
pytest>=8,<9
